# Which backend this build talks to.
#
# Unset falls back to the test backend, which is what local development wants.
# A production build MUST set this -- see eas.json's production profile -- or
# it ships pointing at test, with real riders' deliveries and earnings going
# to a staging database.
EXPO_PUBLIC_API_URL=https://test.api.marktcommerce.com
